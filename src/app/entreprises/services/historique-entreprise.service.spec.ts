import { TestBed } from '@angular/core/testing';

// import { HistoriqueEntrepriseService } from './historique-entreprise.service';

describe('HistoriqueEntrepriseService', () => {
  // let service: HistoriqueEntrepriseService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(HistoriqueEntrepriseService);
  // });

  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
